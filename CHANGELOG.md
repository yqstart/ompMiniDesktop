# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号遵循语义化版本。

## [Unreleased]

### Added
- **左栏项目行可移除项目**：项目行 hover 出现「移除项目」（`Trash2`，hover 转 danger），走 `ConfirmDialog` 二次确认（标题带项目名，正文写明「只解除绑定，不删除任何文件；该项目下的会话会被标记为已归档」），确认后调 `remove_project` 并刷新左栏——这条后端命令 V1 起就有，但一直没有 UI 入口。
- **项目文件夹图标标识「当前打开的项目」**：项目头 24px 的文件夹图标在活动工作区落在本项目里（右栏正在显示它的终端）时上强调色，其余保持灰阶；缺失目录仍走 warn。

### Changed
- **设置页不再被 `omp models --json` 拖住**：这条命令实测 **2–10s**（上游每次都要拉各供应商的模型目录，抖动大），而设置页会并发触发它三次——启动预取、健康检查、供应商页各一次——模型页还要等它和角色 / 切换环 / 转移链一起返回才渲染，这就是「模型、供应商有时要等 6–7 秒」的来源。现在模型目录只剩**一个读取入口**：单飞（`models_fetch` 锁，同一时刻至多一个进程，其余调用方等同一份结果；阻塞子进程挪进 `spawn_blocking`）+ 5 分钟缓存 + **过期后台刷新**（stale-while-revalidate：先拿旧快照渲染，新快照完成即广播 `omp-models://catalog`，App 换 `store.models`、供应商页重算「已配置」）；健康检查与供应商页直接用快照、不等目录，供应商清单与目录并发。配套：模型页的四个读取**各自到达即渲染**（目录不再拖着角色 / 切换环 / 转移链）；`modelRoles` / `modelRoleStorage` / `cycleOrder` / `retry.*` 六个键的读取从「每键一次 `omp config get`」合并成**一次 `omp config list --json`**（0.14s 拿全量 500+ 键，值与 `config get` 同构），读取统一钉在 agentDir（与写入同层）。
- **应用图标收敛成单一色相带**：π 字标原来走青绿 → 天蓝 → 紫 → 粉的四色「极光」渐变（色相跨度 158°、含粉色），在 Dock 里跟邻居（无彩立方体、单色紫 M、白 `>_` + 柠檬绿）并排时是一道彩虹，底板还被三处彩色辉光染成偏色。现在 π 改成蓝 → 靛单色带（`#6FB4FF → #9C93FF`，211° → 245°，中点 ≈ 应用强调色），底板换成近中性石墨 + 两处同色微辉光。字形、几何、圆角与 16px 可读性都没动（源仍是 `design-system/icon/omp-mini-icon.svg`，`pnpm icon` 重生成）。

### Removed
- **壳侧不再管理 worktree（只保留只读展示）**：项目头的「新建 worktree」（分支过滤 + 新建分支 + 基线两档）与「项目操作」（清理失效登记 / 清理孤儿 worktree）、worktree 行 hover 的「删除」（探脏二次确认 + `force` 重删）一起退场，连带删除 6 个后端命令——`create_worktree` / `remove_worktree` / `prune_worktrees` / `list_orphan_worktrees` / `clear_orphan_worktrees` 与 `get_git_info`（唯一使用者是新建面板），以及 `GitInfo` / `OrphanWorktree` / `OrphanClearResult` 类型与 27 个字典键。**展示照旧**：左栏仍列每个项目的全部 git worktree（真相 = `git worktree list`），分支名、worktree 徽章、改动点 / 领先·落后 / 上游缺失徽章与「提交…」入口都在；新建 / 删除 worktree 走 omp 与 git 命令行，worktree 里的会话归属不受影响。

## [0.3.0] - 2026-09-24

### Added
- **提交面板：文件勾选 + 可编辑提交信息 + 一键「提交并推送」**：左栏工作区行 hover 的按钮现在**打开提交面板**（不再直接开跑）：面板里列出本次改动（状态 chip `M`/`A`/`D`/`R`/`??` + `+N −M`），**勾选决定这次提交包含哪些文件**（默认勾选已暂存的那批，一个都没暂存则全选）；「生成提交信息」出的信息落进**可编辑**的文本框，改完再点「提交」或「提交并推送」。以往只有一条链路——`omp commit` 自动 `add -A` 提交全部改动、信息不可改。
- **worktree 生命周期补齐**：worktree 行 hover 多一个「删除」（先探脏：有未提交改动时用后端给的「N 个文件」做二次确认，确认后强删；有终端开在该目录时直接拒绝）；项目头的「…」菜单里可「清理失效登记」（`git worktree prune`，目录被手工删掉的那些）与「清理孤儿 worktree」（`omp worktree clear`，`~/.omp/wt` 全域，确认框会写明范围）；新建 worktree 时还能选**基线**——「当前 HEAD」或「远端最新（`origin/main`）」，后者会先 `git fetch` 再以远端默认分支为起点。

### Changed
- **生成提交信息从 16–35 秒降到 3.5 秒（中位）**：以前点一次要跑完 `omp commit` 的整条 agent 流水线（刷新模型注册表 → 检测 changelog → 发现上下文文件 → 多轮工具调用的 commit agent），**实测 16–35s，空跑一次也要 ~10s**。现在默认走壳侧**单轮**生成：取暂存区 diff → 一次 `omp -p`（`--no-session --no-tools --no-lsp --no-extensions --no-rules`，模型与思考档取 `modelRoles.commit`）→ 输出流式进编辑框；三次实测 5.13s / 2.63s / 3.52s。完整链路仍在：面板切到「完整（含 CHANGELOG）」走 `omp commit`（会维护 `CHANGELOG.md`、保留拆分提交，但仍慢）。两条轨道都**遵守勾选**。
- **提交 / 推送的 git 写操作回到壳侧**：`git add` / `git commit` / `git push` / `git worktree remove` 由壳侧用 git CLI 直接执行（快、错误就是 git 原文），只有「完整提交」才 spawn `omp commit`。推送在没有上游时自动 `git push -u origin <branch>`（唯一 remote 也行，多个且无 origin 时明确报错）；凭证不可用给出「终端里先手动 push 一次」的提示。
- **提交语言与「记住选择」两轨共用**：三档（系统默认 / 中文 / English）解析出的要求文本，快速轨拼进提示词、完整轨走 `omp commit --context=`；中文档的说明文案从「按校验器要求保留英文过去式动词」改为「摘要首词用英文过去式动词（仓库约定）」。
- **会话弹窗不再要勾选，直接「全部归档 / 全部删除」**：批量归档 / 删除以前要先在每行前打勾、再到底部操作条上选动作——多一步选择、还多一个状态（选中集）。现在行首勾选框、顶部「全选」、底部「已选 N 项 / 清除选择」整组退场，底部只剩两个常驻按钮：**全部归档（N）**（只数当前列表里还没归档的那些，没有就禁用）与**全部删除（N）**（覆盖列出的每一行，仍走二次确认）。作用域 = **当前列表**：搜索框里输了词，就作用于搜出来的那批（按钮上的数量即作用域）。行内的单条归档 / 恢复 / 删除不变。
- **设置 ›「常用设置」的枚举项改成下拉浮层**：`defaultThinkingLevel`、`tools.approvalMode` 这类多值设置以前在行下面展开一整块选项列表，把后面的设置整体推走、也让那一段页面变成常驻的选项区。现在点行尾按钮弹出贴着它的浮层（`EnumSelect`），选项不再进入设置页的文档流；浮层挂在 `body` 上（设置页的内容区是唯一滚动容器，行内浮层会被裁掉），下方放不下且上方更宽裕时向上弹，滚动 / 缩放时跟着触发按钮走。`Esc` 与点击外部关闭、关闭后焦点回到按钮，再点触发按钮即可收起——`useDropdown` 新增 `ignore`（触发按钮吃自己的点击），顺带修掉「同一次点击被 `pointerdown` 关掉、又被 `click` 打开」导致按钮点不掉的老毛病。

### Removed
- **左栏行「提交并推送」按钮不再直接开跑**：点它是打开面板（ahead 徽章的「点击即推送」保持不变）；后端那条「预检决定跑提交还是推送」的分支（`decide`）随之删除——改由面板的变更集与提交前的暂存复查裁决。

## [0.2.1] - 2026-09-23

### Added
- **会话标题按界面语言生成（中文 / 英文）**：自动会话标题此前由 omp 的模型自由发挥（默认英文）。现在壳把界面语言同步成 omp 的标题生成提示词——写 `<agentDir>/TITLE_SYSTEM.md`（omp 在会话启动时读取），中文界面出的新标题就是中文、英文界面就是英文。用户自己写过的同名文件**不会被覆盖**；项目里若有 `<项目>/.omp/TITLE_SYSTEM.md` 仍以它为准（上游的项目级优先规则）。对**新开的终端**生效，已在跑的会话保持原语言（omp 只在会话启动时读该文件）。

### Changed
- **终端标签只显示会话标题**：标签里原本第二行的 cwd 路径去掉了（窄标签里必然截断成 `/Users/yanqi/Des…`，只剩噪音），现在标签是**单行**——π 状态标 + 会话标题；完整路径仍可在标签的悬停提示里看到。双击改名的失败原因仍显示在名字行下方（编辑期）。
- **应用内更新弹窗显示这个版本改了什么**：之前 `latest.json` 的 `notes` 只有一句「见 CHANGELOG.md。」，弹窗里等于什么都没说。现在发版工作流用 `scripts/changelog-notes.mjs` 抽出 CHANGELOG 当前版本那一节，既当 Release 说明、也在末尾的 fixup job 回填进 `notes`；弹窗把这段 Markdown 渲染成「本次更新」块（小节标题 / 列表 / 加粗 / 行内代码，超长可滚动，链接交给系统浏览器），`notes` 为空时整块不出现。版本节缺失或为空会让发版直接失败——「发版前忘了写 CHANGELOG」挡在工作流里；存量 release 也可用同一脚本回填。顺手修掉弹窗里过期的「稍后可在顶栏重新打开」（顶栏 Bell 已随 V11 退场），改成真实入口「设置 › 关于」。
- **设置页左栏按「omp / 本应用」分组，两页的快捷定位条删除**：原来「通用」一页里混着两类东西——`omp config` 的 41 项白名单，和本应用自己的更新检查 / 运行环境诊断；现在拆成 **常用设置**（omp 白名单 + 搜索）与 **关于**（本应用的更新 + omp 运行环境诊断），左栏按对象分成两组、各带组标题（上组 omp：常用设置 / 模型 / 记忆 / 供应商用量；下组本应用：关于 / 使用统计 / 已归档对话），组标题在窄导航（44px）下自动收进 `sr-only`，分组语义仍由导航结构保留。同时删掉「通用」「模型」两页顶部那排快捷定位 chips——页里的区块本来就不多，左栏切页即导航；更新弹窗里「设置 › 通用」的指向文案同步改为「关于」。

## [0.2.0] - 2026-09-23

### Added
- **左栏工作区行的终端数徽章**：每行标出该工作区开着几个终端（其中有进程在跑时上强调色），一眼看出别的分支还有没有在跑的东西。
- **会话弹窗的批量归档 / 删除**：会话行前多一个勾选框，勾选后底部出现操作条——归档 / 恢复只作用于适用项（各自按选中集过滤，没有适用项时禁用），删除走同一个二次确认；「全选」作用在当前过滤结果上。批量与单条共用同一份分批与失败聚合（`lib/sessionBatch.ts`）。

### Changed
- **终端标签标的是会话标题，不是项目名，并且可以改**：omp 在会话还没有标题时会把工作区目录名当标题广播——壳侧把这种回退值认出来（不显示成标题，标签退回「项目 · 分支」），会话标题一到位就顶上。**双击标签即地改名**：走 omp 原生命令 `/rename`（先清输入行再注入整行命令，`omp` 自己写回会话并广播新标题，壳侧不留本地覆盖）——会话正忙（`π` 不是等待输入）或标题非法（空 / 含控制字符）时拒绝注入并写明原因。
- **工作区 git 快照的刷新时机补两个**：窗口重新获得焦点、以及 30 秒兜底轮询（仅窗口可见时跑；上一轮没回来就跳过这一轮）。此前只有「omp 干完活 / 窗口转可见 / 任务结束」等时机——在终端里手敲 git 命令或 agent 中途提交时，左栏的改动点 / 待推送徽章会一直停在旧值。
- **终端视图按左栏选中的工作区过滤**：右栏的标签栏、面板与 `⌘1..9` 只列**当前工作区**的终端（`cwd` 精确匹配），不再把各分支的终端混在一起。别的分支的终端照常运行、不被打断——左栏徽章提示它们还在，`⌘⇧K` 快速切换仍能全局跳过去（跳过去左栏也跟着切）。关闭终端后的相邻聚焦、`⌘T` 的新建目录、工作区失效回退都收敛到同一口径；切到没有终端的工作区给空态，可当场在该分支新建。

## [0.1.0] - 2026-09-22

首个公开发布：oh-my-pi（`omp`）的极简桌面壳——**终端工作区形态**。

左栏是项目 → 工作区（主目录 + git worktree）树，右侧是 omp 终端标签页；
**每个终端就是一个 omp 会话**——壳侧把 `omp` 自己的 TUI 原样跑在 PTY + xterm 里，
不解析、不翻译终端字节流（会话的渲染、审批、斜杠命令都在 omp 的 TUI 内完成）。

### 新增

- **终端工作区**：常驻标签栏（终端标签 + 设置标签 + `＋`），点工作区行即在该目录起 `omp --cwd <dir>`（该目录已有终端则聚焦它），`⌘T` 新建、`⌘W` 关闭、`⌘1..9` 切标签。标签上是「π 状态标 + 会话名」，名字与状态取自 omp 的 OSC 标题（工作中 / 等你确认 / 轮到你）；状态另有 `sr-only` 文本与悬停提示，颜色不作唯一信号。
- **会话弹窗**：项目行的会话入口列出该项目（含 worktree）的全部会话，点行即 `omp --resume <id>` 在终端里继续；归档 / 恢复 / 删除在同一弹窗内完成（删除走二次确认）。
- **worktree 原生支持**：worktree 真相 = git（手工 `git worktree add` 的也列出）；创建走 `omp worktree add`，路径按 omp 既有约定落在 `~/.omp/wt/<repo>-<branch-slug>`，已检出的分支幂等复用；主目录与 worktree 在树上分色。
- **工作区「提交并推送」（`omp commit` 集成）**：两段式——先 `omp commit`（生成提交信息 + 维护 changelog，只提交），浮层里过目信息后点「推送」走无改动快路径；任务按 cwd 建表、不同工作区可并行，关闭浮层转后台（行徽章指示），取消与应用退出打进程组收尾。行的脏工作区 / 领先·落后远程 / 上游缺失徽章来自 git 快照。
- **设置页六个页签**：通用 · 模型 · 记忆 · 使用统计 · 供应商用量 · 已归档对话。这是全 app 唯一改 omp 状态的地方（本应用的偏好另有落点），以标签栏里的单例标签存在：左栏底部入口打开 / 聚焦，`⌘W` 关闭并回到上次的终端。
  - **通用** = `omp config` 白名单（41 项）+ omp 诊断（路径 / 版本 / agentDir 展示与复制、重新检测、手动指定可执行文件）。
  - **模型** = 「供应商 → 我的模型 → 模型角色 → 快速切换环 → 失败转移」一条动线。添加供应商是可搜索的提供商选择器：API key / OAuth 走 `auth-broker login`，首项「自定义」写 `<agentDir>/models.yml`（保真编辑：注释与界面之外的字段原样保留；后端 hash 乐观锁 + 预校验 + 备份 + 原子写四道闸）。「挑选模型」星标进「我的模型」，它同时是模型角色与失败转移的候选范围；快速切换环可视化 omp 终端 Ctrl+P 的 `cycleOrder`（条目是角色 id，整组覆盖写）。
  - **记忆** = 列 / 读 / 删 omp 项目记忆（只删不写）。
  - **使用统计** = tokens 用量 / Cache 命中率 / 活跃天数三张卡 + GitHub 贡献图口径的「Token 活动」热力图（每日 / 每周 / 累计三档只换取值口径，悬停按模型拆分，0 用量的格子不出浮层）。
  - **供应商用量** = 各供应商侧的滚动窗口（5 小时 / 每周 / 每月）进度条、百分比、重置倒计时与套餐 / 多账号标识，数据取 `omp usage --json`；omp 未实现探针的 commandcode / deepseek 由壳侧补充查询（凭据只经 `omp token` 内存传递、不落盘、不回传前端），查询失败与「上游没有用量查询」分别列出并写明原因。
  - **已归档对话** = 归档的唯一管理面（不受扫描窗口限制），恢复 = 取消归档 + 终端 resume，行级与分组级都可删除。
- **内嵌终端图标字体与 `symbolPreset`**：单宽（0.6 em = 1 个终端 cell）的 Nerd Font 图标字体挂在 `--font-mono` 末尾，只补图标码点，ASCII / 中文仍走系统字体；设置 › 通用可切「Unicode 符号 / Nerd Font 图标 / 纯 ASCII」三档，切到 nerd 档后 omp 欢迎头那条「Please use nerdfont」提示不再出现。
- **应用内更新**：tauri updater + 四平台 Release 产物 + `latest.json` 公开直链——Release 工作流末尾把资产 API 链接统一改写为 `releases/download` 直链（匿名限流 60 次/小时/出口 IP，会让应用内下载报 403「能检查、不能下载」），发版前的守卫 job 挡掉仓库私有 / 签名缺失 / tag 与版本不一致三种坏发布。
- **界面**：冷灰 / 石墨蓝双主题（跟随系统 / 深色 / 浅色）+ 中英双语文案（跟随系统 / 中文 / EN），都挂在左栏底部「设置」行右侧；左栏可拖宽、窄窗收成抽屉；主区是常驻标签栏 + 内嵌圆角工作面，切换标签只切显隐——终端 xterm 实例与滚动缓冲、设置页的页签选择与滚动位置都保留。

### 修复

- **macOS 下 Shift 组合键（`?` `@` `"` …）要按两遍、中文输入法首击被吞**：WKWebView 把字符的 `input` 排在字符自身 keydown 之前，xterm 的去重标记还记着修饰键而误杀真实输入，随后的 keyCode 229 composition 兜底又读到空差值（上游 xterm.js #5374 未修）。壳侧在冒泡阶段补一层：只补 xterm 自身规则会拒绝、且 keypress 也没发的那一次，IME 组字 / 删除 / 换行 / 粘贴与读屏模式一律不动，非 macOS 不挂载。
- **正常字符被打出两个**（逗号、引号、空格双发）：早期补丁用 `defaultPrevented` 判「xterm 已消费」，而 xterm 默认 `cancelEvents=false`、消费成功也不 `preventDefault`——现在只读 xterm 自己的接受条件，补发只走公开 `input()`，不动 textarea 内容。
- macOS 反复弹「“ompMiniDesktop”想访问“桌面”文件夹」：授权框改为中文用途说明（adhoc 签名下 TCC 按签名身份记授权，根治需 Developer ID 签名 + 公证）。
- 应用内「检查更新」不再因仓库私有而失败（Release 工作流新增仓库可见性守卫）。
- 终端里不再绘制滚动条（xterm 6 自绘滚动条是 DOM overlay）；标签溢出时垂直滚轮映射为横向滚动，滚轮 / 键盘 / 回看缓冲区不受影响。

### 变更

- 终端标签**不与 jsonl 会话做运行时绑定**：会话 id 由 omp 的 TUI 自己创建，`--resume` 由会话弹窗发起（cwd 用会话原目录）。
- 键位调整：面板内是 omp 的 TUI 全屏交互，终端标签用 `⌘T` 新建、`⌘W` 关闭、`⌘1..9` 切换。
